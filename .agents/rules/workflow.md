# Workflow & Coding Rules

- When the user asks for code changes, perform ONLY the requested code edits directly in the files.
- DO NOT launch browser subagents, open browsers, or perform browser automated testing/screenshots unless the user explicitly asks for visual testing.
